let numSec = parseInt(Math.random() * 10 + 1);
let numDig = prompt('Digite um numero entre 1 e 10:');
if (numSec === numDig) {
    prompt('Parabens você acertou!')
} else {
    prompt('Que pena, tente novamente!')
}
